{
  "summary": "What's New opens instantly — heavy startup work runs on the splash screen with visible progress.",
  "introduced": [],
  "changed": [
    "Startup splash shows step-by-step progress for loading saved scans, checking the AI provider, and preparing suggestions",
    "Main window stays empty until What's New is dismissed so tabs and charts do not build behind the overlay",
    "Skip loading saved scan after 3 seconds without blocking the rest of startup"
  ],
  "fixed": [
    "What's New and Continue no longer beach-ball — analysis, AI refresh, and UI initialization moved off the overlay path"
  ]
}
