# Workflow: cut release

1. Follow `prompts/release.md` / `/release`.
2. Curated `release-notes/{version}.json` before `npm run release`.
3. Push the version-bump commit and `v{version}` tag after a successful release.
